"use client";

import ComposeForm from "@/components/ComposeForm";

export default function ComposePage() {
  return (
    <div className="h-full bg-[var(--card)] animate-fade-in">
      <ComposeForm />
    </div>
  );
}
