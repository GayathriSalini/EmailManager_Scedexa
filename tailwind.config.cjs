// tailwind.config.cjs was added earlier but the workspace has reverted the CSS changes.
// Leaving an empty config to avoid affecting builds. Delete this file if you want it removed.
module.exports = {};